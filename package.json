{
  "name": "wintermute",
  "version": "1.0.0",
  "scripts": {
    "heroku-prebuild": "cd frontend && npm install --include=dev",
    "heroku-postbuild": "cd frontend && npm run build",
    "start": "node_modules/.bin/serve -s frontend/dist -l $PORT"
  },
  "engines": {
    "node": "18.x"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ],
  "devDependencies": {
    "@types/node": "^20.11.16",
    "http-server": "^14.1.1",
    "path": "^0.12.7",
    "serve": "^14.2.4"
  }
}
