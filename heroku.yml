# heroku.yml
build:
  docker:
    web: Dockerfile  # This tells Heroku to use the Dockerfile for the 'web' process
run:
  web: gunicorn main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --timeout 120 --bind 0.0.0.0:$PORT